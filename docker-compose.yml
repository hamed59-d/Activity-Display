version: "3.9"

services:
  activity-backend:
    build: ./backend
    container_name: activity-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    volumes:
      - activity_data:/app/data

volumes:
  activity_data: